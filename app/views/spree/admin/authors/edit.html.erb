<% content_for :page_actions do %>
  <li>
    <%= link_to t('spree.new_author'), new_object_url, class: 'btn btn-primary' %>
  </li>
<% end if can?(:create, Spree::Author) %>

<%= render partial: 'spree/shared/error_messages', locals: { target: @author } %>

<% admin_breadcrumb(link_to plural_resource_name(Spree::Product), spree.admin_products_path) %>
<% admin_breadcrumb(link_to plural_resource_name(Spree::Author), spree.admin_authors_path) %>
<% admin_breadcrumb(@author.name) %>

<%= form_for [:admin, @author], method: :put, html: { multipart: true } do |f| %>
  <fieldset class="no-border-top">
    <%= render partial: 'form', locals: { f: f } %>
    <%= render partial: 'spree/admin/shared/edit_resource_links' %>
  </fieldset>
<% end %>
